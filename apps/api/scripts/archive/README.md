# Archived API Scripts

This folder keeps old one-off operational scripts out of the top-level script inventory without deleting their history.

Archived scripts should not receive package aliases. If one becomes a current workflow again, move it back to `apps/api/scripts/`, document its safety posture, and add or update tests in the same focused change.
